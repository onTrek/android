package com.ontrek.mobile.screens.profile.components

import androidx.compose.runtime.Composable
import com.ontrek.mobile.screens.profile.ProfileViewModel

@Composable
fun RequestsDialog(
    viewModel: ProfileViewModel,
) {
    // This function will be implemented later
    // It will show a dialog with options to accept or reject a friend request
    // and will display the user's name in the dialog
}